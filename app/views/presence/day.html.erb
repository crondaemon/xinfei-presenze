<div class='container-fluid'>
	<h1 class='page-header text-center'>
		Presenze del giorno:<br/>
		<span class='label label-primary text-center'><%= l(@day, format: '%e %B') %></span>
	</h1>
	<div class='row'>
		<div class='col-md-12'>
	  	<div class='panel panel-default'>
				<div class='panel-body'>
					<%= content_tag 'table', class: 'table table-striped table-hover', id: 'sources-table' do %>
						<thead>
							<tr>
								<th class='day-font hidden-lg hidden-xl'>Nome</th>
								<th class='visible-lg visible-xl'>Nome</th>

								<th class='day-font hidden-lg hidden-xl'>Presente</th>
								<th class='visible-lg visible-xl'>Presente</th>

								<% if current_user.is?(:user_admin) %>
									<th></th>
									<th></th>
								<% end %>
							</tr>
						</thead>
						<tbody>
							<% @users.each do |user| %>
								<tr>
									<td class='click-presence visible-lg visible-xl' data-user='<%= user.id %>' data-day='<%= @day %>'>
										<%= user.fullname %>
									</td>
									<td class='click-presence day-font hidden-lg hidden-xl' data-user='<%= user.id %>' data-day='<%= @day %>'>
										<%= user.fullname %>
									</td>
									<td class='click-presence' data-user='<%= user.id %>' data-day='<%= @day %>'>
										<div id='user-<%= user.id %>-present' class='<%= user.present?(@day) ? "" : "display-none" %>
												col-md-4 col-sm-4 col-xs-6 col-lg-1 col-xl-1'>
											<%= image_tag 'check.png', class: 'img-responsive', height: '100%' %>
										</div>
									</td>
									<% if current_user.is?(:user_admin) %>
										<td><%= link_to('Mostra', user) %></td>
										<td><%= link_to('Disabilita', presence_disable_path(id: user, day: @day)) if current_user != user %></td>
									<% end %>
								</tr>
							<% end %>
						</tbody>
					<% end %>
				</div>
			</div>
		</div>
  </div>
</div>
